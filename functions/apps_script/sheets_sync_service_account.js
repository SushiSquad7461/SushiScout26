/**
 * Google Apps Script - Service Account Auth version
 *
 * DEPRECATED: Use sheets_sync.js instead.
 * This file used a service account for JWT auth, but the simpler API-key
 * approach in sheets_sync.js is sufficient and doesn't require embedding
 * private keys in Apps Script.
 *
 * If you need service-account auth in the future, use Google's
 * OAuth2 library for Apps Script instead of manual JWT signing.
 */
